/**
 * routes/public.js — Public Scan Page
 *
 * Handles the page shown when someone scans a bike's QR code.
 * This is a stub — full implementation coming in the next phase.
 *
 * Routes:
 *   GET /bike/:uid — public scan page (no auth required)
 */

const express = require('express')

const router = express.Router()

// Placeholder routes — will be implemented in the next phase

module.exports = router
