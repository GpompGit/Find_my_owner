/**
 * routes/location.js — GPS Location Endpoint
 *
 * Receives GPS coordinates from the stolen bike scan page.
 * This is a stub — full implementation coming in the next phase.
 *
 * Routes:
 *   POST /api/log-location — receive GPS data (public, no auth)
 */

const express = require('express')

const router = express.Router()

// Placeholder routes — will be implemented in the next phase

module.exports = router
