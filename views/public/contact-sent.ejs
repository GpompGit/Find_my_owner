<%- include('../partials/header', { title: t('public.contact_sent_title') }) %>

<main class="container py-5">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-5 text-center">
      <div class="card shadow-sm">
        <div class="card-body py-5">
          <i class="bi bi-check-circle-fill display-1 text-success"></i>
          <h2 class="mt-3"><%= t('public.contact_sent_title') %></h2>
          <p class="text-muted"><%= t('public.contact_sent_text') %></p>
          <p class="text-muted small">
            <i class="bi bi-shield-lock"></i> <%= t('public.contact_sent_privacy') %>
          </p>
        </div>
      </div>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
